// Component7: Todo List with Delete Feature
// You need:
// • todos state (an array of objects with fields id, text, and completed) 
// • currentTodo state to hold the text of a new todo item
// • handleAddTodo function that adds a new todo to the todos list
// • handle ToggleTodo function that toggles the completed status of a clicked todo handleDelete Todo function that deletes a specific todo based on its id
// • A <form> with an onSubmit for adding todos
// • An <input> inside the <form> CONTROLLED BY STATE for adding new todo
// • A <button> inside the <form> for submitting the form
// • A list (<ul>) that displays all the todos. Each todo (<li>) should:
    // • Be clickable and should toggle its completed status upon clicking.
    // • Have a delete (<button>) next to it. When clicked, it should remove the todo from the list using handleDeleteTodo.